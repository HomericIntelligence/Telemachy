# Minimal ignorefile for the CI image build context.
# CI tool manifests remain in the context; project dependencies are installed
# from the mounted source's lock file when a check needs them.
.git
.gitignore
.github
docs
*.md
node_modules
.venv
.pixi
.ci-pixi
ci/node_modules
